#include "holberton.h"
/**
 * puts_half - Prints half of string.
 * @str: character.
 * Return: Always 0.
**/
void puts_half(char *str)
{
	char aux;
	int i, n, x, cont = 0;

        for (i = 0 ; aux != '\0' ; i++)
        {
		cont++;
        }

	if (!(cont % 2 == 0))
	{
		n = (cont - 1)/2;
	}
	



4
	_putchar('\n');
}
